# 测试账号说明

> 当前云端数据如果已经清空，以下账号需要先按新鉴权方案重新注册后才能使用。

## 1. 预留房东账号

- 手机号：`13387395714`
- 密码：`13387395714A`
- 建议角色：`landlord`

## 2. 预留租客账号

- 手机号：`17364071058`
- 密码：`17364071058A`
- 建议角色：`tenant`

- 手机号：`18302096587`
- 密码：`18302096587A`

## 3. 使用注意事项

- 手机号注册 / 登录不会自动绑定微信身份
- 如果要测试“绑定微信”能力，请显式在个人中心触发绑定
- 如果要测试双账号聊天并同时覆盖微信绑定能力，请使用两个不同的微信运行身份
- 不要把同一个微信身份绑定到两个业务账号
- 如果联调脚本反复跑过，`users` 里可能会残留这两个手机号的 `disabled` 历史记录；开发环境可执行 `npm run cleanup:test-users` 做清理
